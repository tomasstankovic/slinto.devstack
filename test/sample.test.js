/**
 * Sample unit test.
 */
 
var assert = require('assert');

describe('Sample test', function() {
	describe('#mySampleFunction()', function() {
		it('should be equal', function() {
			assert.equal(1, 1);
		});
	});
});
